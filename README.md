# TASK_52

The **VetOps** application and Docker stack live under **[`repo/`](./repo/)** (`backend/`, `frontend/`, Compose files, and the test harness).

- **Quick start:** `cd repo` then follow [`repo/README.md`](./repo/README.md).
- **Docs** at clone root: [`docs/`](./docs/) (when present).
- **Metadata:** [`metadata.json`](./metadata.json).
