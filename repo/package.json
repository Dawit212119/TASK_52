{
  "name": "vetops-orchestration",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "bootstrap": "node ./scripts/bootstrap.mjs",
    "up": "docker compose up -d --build",
    "down": "docker compose down",
    "reset:mysql-volumes": "node ./scripts/reset-mysql-volumes.mjs",
    "logs": "docker compose logs -f --tail=200",
    "ps": "docker compose ps",
    "rebuild": "docker compose build --no-cache",
    "migrate": "docker compose exec backend php artisan migrate --force",
    "seed": "docker compose exec backend php artisan db:seed --force",
    "test:backend": "docker compose exec backend php artisan test",
    "test:frontend": "docker compose run --rm frontend-dev npm run test",
    "test": "npm run test:backend && npm run test:frontend"
  }
}
